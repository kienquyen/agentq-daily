# AgentQ V5.5 — AI Signal Bot
# Pinned to versions confirmed working on 2026-05-17

# ── Core Discord ──────────────────────────────────────────────────────────────
discord.py>=2.3.0
python-dotenv>=1.0.0

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9

# ── Data & ML ─────────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=10.0.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
joblib>=1.3.0
ta>=0.10.0

# ── Vnstock (free tier — OHLCV fallback) ─────────────────────────────────────
vnstock>=4.0.0
vnstock_ezchart>=0.0.3

# ── Vnstock sponsored packages ────────────────────────────────────────────────
# NOTE: vnstock_data is bundled as local code in the repo (vnstock_data/ folder)
# vnstock_news, vnstock_ta, vnstock_pipeline are loaded with try/except — optional
# Do NOT add --extra-index-url here: vnstocks.com requires API key auth (not available on Railway)

# ── AI ────────────────────────────────────────────────────────────────────────
google-genai>=2.0.0

# ── Sub-dependencies for vnstock_data ─────────────────────────────────────────
psutil>=5.9.0
pydantic>=2.0.0
requests>=2.30.0
tenacity>=8.2.0
pyyaml>=6.0.0
ssi-fc-data>=1.0.1
matplotlib>=3.7.0
beautifulsoup4>=4.12.0
paho-mqtt>=1.6.1
python-dateutil>=2.8.0

# ── Image generation (PNG recap) ──────────────────────────────────────────────
jinja2>=3.1.0
playwright>=1.40.0
# NOTE: after pip install, run: playwright install chromium
# On Railway this is handled automatically via nixpacks.toml

# ── Dev / Test ────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
