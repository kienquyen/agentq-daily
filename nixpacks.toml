# nixpacks.toml — Railway build config

[phases.setup]
nixPkgs = [
  "gcc",
  "git",
]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
]

[start]
cmd = "python scheduler/run_daily.py"
